# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

                           |-----handy-ruler------------------------------------------------------|
gnome-directory-thumbnailer: gnome-directory-thumbnailer
gnome-directory-thumbnailer:
gnome-directory-thumbnailer: gnome-directory-thumbnailer is a GNOME thumbnailer utility which will
gnome-directory-thumbnailer: generate a thumbnail for a directory. It is intended to be called by
gnome-directory-thumbnailer: gnome-desktop’s thumbnailer code, but can be called manually as well.
gnome-directory-thumbnailer:
